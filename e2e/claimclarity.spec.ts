import { test, expect } from "@playwright/test";
test("public sample path works without login", async ({ page }) => { test.setTimeout(120000); await page.goto("/"); await page.getByRole("button", { name: /try a sample claim/i }).click(); await page.getByRole("button", { name: /analyze this claim/i }).click(); await expect(page.getByRole("heading", { name: "Under Process" })).toBeVisible({ timeout: 60000 }); await expect(page.getByText("Why this looks confusing")).toBeVisible(); await expect(page.getByText(/different stages/i)).toBeVisible(); await expect(page.getByText("Timeline")).toBeVisible(); await expect(page.getByText("What should I do?")).toBeVisible(); await expect(page.getByText(/Don.t do this yet/)).toBeVisible(); await page.getByRole("button", { name: /reset demo/i }).click(); await expect(page.getByRole("button", { name: /try a sample claim/i })).toBeVisible(); });
